{
  "name": "fullstack-rescript",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "res:build": "bun run --cwd packages/shared build && bun run --cwd packages/backend build && bun run --cwd packages/frontend build",
    "res:clean": "bun run --cwd packages/shared clean && bun run --cwd packages/backend clean && bun run --cwd packages/frontend clean",
    "dev:backend": "bun run --cwd packages/backend dev",
    "dev:frontend": "bun run --cwd packages/frontend dev",
    "build": "bun run res:build && bun run --cwd packages/frontend build"
  },
  "devDependencies": {
    "rescript": "^12.0.0"
  }
}
